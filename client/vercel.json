{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node"
        }
    ],
    "scripts": {
        "build": "npm install --legacy-peer-deps && npm run build"
    }
}